>`Duration` 是许多编程语言和框架中用于表示时间段（时间间隔）的一个类或结构体。它在处理时间相关的计算、延迟、动画、定时器等场景中非常有用。

## 🌟 一、`Duration` 的基本概念

`Duration` 表示一个时间间隔，而不是一个具体的时间点。它通常包含以下信息：

- 天数（days）
- 小时（hours）
- 分钟（minutes）
- 秒（seconds）
- 毫秒（milliseconds）
- 微秒（microseconds）

例如：

```java
Duration(seconds: 5) // 表示5秒的时间间隔
```
