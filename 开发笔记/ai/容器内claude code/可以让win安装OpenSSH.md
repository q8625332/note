> 安装这个就可以让wsl容器里的docket也可以访问宿主主机

宿主主机需要安装 OpenSSH server的服务。

让容器内的claude code访问 通过ssh访问宿主主机，如何行不通，在宿主主机上使用其他ai协助解决这个问题。

最后让他能免密登录就行。

效果如下：
![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/de89d482026446789ab14cc50abe84fa.png)

就算打通了。